# Python deps for tools/convert_minilm.py
# One-time install — does not affect Android build.
# Recommend a fresh venv: python -m venv .venv && source .venv/bin/activate

torch==2.4.1
transformers==4.45.2
onnx==1.17.0
onnx2tf==1.26.3
tensorflow==2.17.0
# TF 2.16+ ships Keras 3 by default; onnx2tf uses the Keras 2 API.
tf-keras==2.17.0
# onnx2tf transitively needs these; pinning to avoid surprises
sng4onnx>=1.0.1   # naming generator
sne4onnx>=1.0.13  # subgraph extraction (used by -cotof verification)
onnxsim>=0.4.36
onnx_graphsurgeon>=0.5.2
onnxruntime>=1.18 # runs ONNX inference for -cotof output check
psutil>=5.9
